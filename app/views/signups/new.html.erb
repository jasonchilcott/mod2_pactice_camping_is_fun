<h1>New Signup</h1>

<% if flash[:signup_errors] %>
    <% flash[:signup_errors].each do |e| %>
        <%= e %>
    <% end %>
<% end %>

<%= form_for @signup do |f| %>
<%= f.label :camper %>
<%= f.collection_select :camper_id, Camper.all, :id, :name %>
<%= f.label :activity %>
<%= f.collection_select :activity_id, Activity.all, :id, :name %>
<%= f.label :time %>
<%= f.select :time, (0..23) %>
<%= f.submit %>
<% end %>